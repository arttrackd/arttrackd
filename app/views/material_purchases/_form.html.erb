<%= form_for(@material_purchase) do |f| %>
  <% if @material_purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@material_purchase.errors.count, "error") %> prohibited this material_purchase from being saved:</h2>

      <ul>
      <% @material_purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :user_id, value: @current_user.id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :cost %><br>
    <%= f.number_field :cost, :class => 'text_field', :step => 0.01 %>
  </div>
  <div class="field">
    <%= f.label :units %><br>
    <%= f.number_field :units, :class => 'text_field', :step => 'any'%>
  </div>
  <div class="field">
    <%= f.label :receipt, "Upload a receipt for this purchase" %><br>
    <%= f.file_field :receipt %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
