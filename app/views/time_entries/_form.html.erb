<%= form_for(@time_entry) do |f| %>
  <% if @time_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_entry.errors.count, "error") %> prohibited this time_entry from being saved:</h2>

      <ul>
      <% @time_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :project_id, value: @time_entry.project_id || "" %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time, value: @time_entry.start_time.strftime("%B %d, %Y") || "" %>
  </div>
  <div class="field">
    <%= f.label :stop_time %><br>
    <%= f.datetime_select :stop_time, value: @time_entry.stop_time.strftime("%B %d, %Y") || "" %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date, value: @time_entry.date || "" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
