  <div class="business-show-name"><%= @business_expense.name %></div>
  <br>

  <div class="business-show-description gd"><%= @business_expense.description %></div>
  <div class="business-show-amount gd">Amount: <%= number_to_currency(@business_expense.amount) %> </div>
  <div class="business-show-recurring gd"><% if @business_expense.recurring %>Recurring<% else %>Not recurring<% end %></div>

<div class="edit-back">
  <%= link_to (image_tag asset_url('edit.png'), class: "index-icon"), edit_business_expense_path(@business_expense) %> |
  <%= link_to 'Back to Business Expenses', business_expenses_path %>
</div>
