<h1>Editing <%= @business_expense.name %></h1>

<%= render 'form' %>

<div class="edit-back">
  <%= link_to 'Details', @business_expense %> |
  <%= link_to 'Back', business_expenses_path %> |
  <%= link_to 'Delete', business_expense_path, method: :delete, data: { confirm: 'Are you sure?' } %>

</div>
