<p id="notice"><%= notice %></p>

<h1>Listing Sale Goals</h1>
<% @goals.each do |goal| %>
  <div class="goal-amount">Goal: <%= number_to_currency(goal.amount) %></div>

  <div class"goal-details">
    <div class="goal-length_of_time"> Duration: <%= goal.length_of_time %></div>
    <div class="goal-start-time"> Started: <%= goal.start_time %></div>
    <div class="goal-success">Achieved: <%= goal.success %></div>
  </div>

  <div class="goal-edit">
    <%= link_to 'Show', goal %>
    <%= link_to 'Edit', edit_sales_goal_path(goal) %>
    <%= link_to 'Destroy', goal, method: :delete, data: { confirm: 'Are you sure?' } %>
  </div>
  <br>
<% end %>

</br>

<%= link_to 'New Sales goal', new_sales_goal_path %>
