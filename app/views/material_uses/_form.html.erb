<%= form_for(@material_use) do |f| %>
  <% if @material_use.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@material_use.errors.count, "error") %> prohibited this material_use from being saved:</h2>

      <ul>
      <% @material_use.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :material_purchase_id, value: @material_use.material_purchase_id || "" %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, value: @material_use.name || "" %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, value: @material_use.description || "" %>
  </div>
  <div class="field">
    <%= f.label :units %><br>
    <%= f.number_field :units, :class => 'text_field', :step => 0.01, value: @material_use.units || "" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
